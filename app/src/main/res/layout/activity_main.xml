<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.vkaryagin.testaplication.MainActivity">

    <ScrollView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/scrollView"
        android:layout_alignLeft="@+id/urlField"
        android:layout_alignStart="@+id/urlField"
        android:layout_alignParentBottom="true"
        android:layout_alignRight="@+id/urlButton"
        android:layout_alignEnd="@+id/urlButton"
        android:layout_below="@+id/urlButton">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:scrollIndicators="right"
            android:id="@+id/textHTML"
            android:layout_alignTop="@+id/scrollView"
            android:layout_centerHorizontal="true" />
    </ScrollView>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Connect"
        android:id="@+id/urlButton"
        android:layout_alignTop="@+id/urlField"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/urlField"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:text="URL" />

    <!--android:layout_alignRight="@+id/textHTML" -->
        <!--android:layout_alignEnd="@+id/textHTML" -->

</RelativeLayout>
